# First deploy — systemd (bare VPS)
#
# Edit the four placeholders below, then paste this entire file
# to your AI agent (omp.sh, Claude Code, Cursor, etc.)
#
# Placeholders to replace:
#   USER@SERVER_IP       → your SSH login (e.g. root@203.0.113.5)
#   YOUR_USER/YOUR_REPO  → your GitHub repo (e.g. maulanashalihin/laju-go)
#   your-domain.com      → your domain (or "none" if no domain yet)
#   /opt/laju-go         → where to install on the server (or keep as-is)

Deploy my Laju Go app on my VPS using systemd (no Docker). Here are
the details:

- SSH: ssh USER@SERVER_IP
- GitHub repo: https://github.com/YOUR_USER/YOUR_REPO.git
- Domain: your-domain.com (or "none" if no domain yet)
- Install path: /opt/laju-go

Steps:
1. SSH into the server.
2. Install Go 1.26+ and Node.js 20+ if not already installed.
3. Clone the repo to the install path.
4. Run `npm install` and `npm run build:all` (this runs vite build then go build).
5. Copy .env.example to .env and change these lines:
   - APP_PORT=8080
   - APP_ENV=production
   - APP_URL=https://your-domain.com (or http://SERVER_IP:8080 if no domain)
   Leave the rest as-is (DB_PATH is relative, works anywhere).
6. The Go binary is named `laju-go` (produced by `go build -o laju-go ./cmd/laju-go`).
   Create a systemd user service at ~/.config/systemd/user/laju-go.service that
   runs `./laju-go` with WorkingDirectory set to the install path.
   Use `systemctl --user` commands and enable lingering with
   `loginctl enable-linger <user>` so the service survives logout.
7. Enable and start the service.
8. Verify: curl http://localhost:8080/health should return 200.
9. Save these three lines to a file called `deploy.env` in my local
   project root (NOT on the server) and add it to .gitignore.
   Use the actual values from above (not the placeholders):
   ```
   DEPLOY_SSH=<the USER@SERVER_IP from above>
   DEPLOY_PATH=<the install path from above>
   DEPLOY_SERVICE=laju-go
   ```
10. Tell me the status and any issues.

# After the agent finishes, your app is live on port 8080.
# For HTTPS, paste this follow-up prompt:
#
#   Now set up a reverse proxy for HTTPS. Use Caddy in front of
#   port 8080, with automatic TLS for your-domain.com.
